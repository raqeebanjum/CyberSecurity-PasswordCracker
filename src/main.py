from src.utils import validate_zip
from src.config import *

def main():
    print("I put the print statements here to get rid of the errors")
    # I'm thinking that we first run the validation checks
    # if there are errors, we print them
    # And after validation is done, we print "ready" or something and then give options for how to crack the password

if __name__ == "__main__":
    main()